apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: payment-service
spec:
  rules:
    - host: payments.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: payment-service
                port:
                  number: 80
